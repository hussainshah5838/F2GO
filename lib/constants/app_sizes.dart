import 'package:flutter/widgets.dart';

class AppSizes {
  static const DEFAULT_PADDING = EdgeInsets.symmetric(
    horizontal: 20,
    vertical: 16,
  );
  static const DEFAULT_HORIZONTAL_PADDING = EdgeInsets.symmetric(
    horizontal: 20,
  );
  static const DEFAULT_HORIZONTAL_VERTICAL = EdgeInsets.symmetric(
    vertical: 16,
  );
}
